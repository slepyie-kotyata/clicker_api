# 🍳 Click & Cook API

Click & Cook — браузерная idle / clicker-игра с кулинарной тематикой.  
Игрок начинает с простых действий и постепенно строит полноценную кулинарную империю: открывает локации, покупает улучшения, автоматизирует процессы и проходит обучение через интерактивный туториал.  
Этот репозиторий содержит серверную часть проекта, реализованную на языке Go, с поддержкой REST и WebSocket API.

---

## 📚 Технологический стек  

- **Язык программирования:** Go
- **Фреймворки:** Echo, Gorilla
- **База данных:** PostgreSQL
- **Кэширование:** Redis
- **Контейнеризация:** Docker
- **Архитектура:** Монолитная (слоистая, modular monolith)
- **API:** REST и WebSocket

---

## ⚙️ Основной функционал

Backend проекта выполняет следующие задачи:

- **Управление жизненным циклом приложения**
  - Корректное завершение работы сервера (graceful shutdown).
  - Автоматическая миграция таблиц базы данных при запуске сервера.

- **Аутентификация и работа с пользователями**
  - Создание и получение учётных данных пользователей из PostgreSQL.
  - Проверка корректности учётных данных пользователя.

- **Безопасность**
  - Валидация и авторизация запросов с использованием JWT (JWT Middleware).
  - Хэширование и безопасное хранение паролей.

- **Взаимодействие в реальном времени**
  - Использование WebSocket для обработки игровых событий.
  - Поддержка нескольких подключений в рамках одной игровой сессии.
  - Broadcast-рассылка сообщений всем подключённым клиентам.

- **Управление игровым прогрессом**
  - Хранение и обновление состояния игровой сессии в PostgreSQL.
  - Кэширование данных сессии в Redis для устойчивости при разрывах соединения.
  - Реализация игровых механик и операций, связанных с игровым состоянием.

---

## 📁 Структура проекта

Проект организован по принципам слоистой архитектуры с разделением ответственности между слоями.

```plaintext
custom_middleware/         #JWT middleware
└── jwt.go

database/                  #Слой доступа к данным
├── autosave.go
├── connector.go
├── db.go
└── operations.go

environment/               #Конфигурация и переменные окружения
└── get_variable.go

handlers/                  #HTTP-обработчики (REST API)
├── refresh.go
└── entry.go

models/                    #Модели данных и структуры БД
├── boost.go
├── level_xp.go
├── password.go
├── prestige.go
├── session_state.go
├── session_upgrade.go
├── session.go
├── upgrade.go
└── user.go

routes/                    #Маршрутизация HTTP и WebSocket запросов
├── entry.go
├── refresh.go
└── ws.go

secret/                    #JWT-секреты из окружения
└── secret.go

seeds/                     #Начальные данные (seed-скрипты)

service/                   #Прикладные сервисы
├── filter.go
├── password_hashing.go
└── jwt_token.go

utils/                     #Вспомогательные функции
└── formatter.go

ws/                        #WebSocket слой (realtime-логика)
├── action.go
├── hub.go
├── message.go
├── passive.go
├── session.go
└── ws_server.go

.dockerignore
.gitignore
Dockerfile
go.mod
go.sum
server.go                  #Точка входа в приложение

```

---

## ▶️ Запуск проекта

Следуйте этим шагам, чтобы запустить проект локально или в Docker.

### 1. Подготовьте необходимые переменные окружения
Создайте файл `.env`:
```dotenv
DB_CONNECTION=<ссылка на PostgreSQL>
REDIS_CONNECTION=<ссылка на Redis>
ACCESS_TOKEN_SECRET=<секретный ключ Access Token>
REFRESH_TOKEN_SECRET=<секретный ключ Refresh Token>
```

### 2. Поднимите PostgreSQL и Redis
Запустите базы данных (можете использовать Docker или самостоятельное развёртывание).

### 3. Запустите сервер
Используйте Docker для сборки и запуска:
```bash
docker build -t clicker_api .
docker run -p 1323:1323 clicker_api
```

### 4. Выполните скрипты
- Выполните скрипт `create_enums` для создания перечислений в базе данных.
- После автомиграции выполните:
  - `create_updates`
  - `create_levels_xps`

---

## 💻 Описание API

Подробные примеры сообщений и сценарии взаимодействия будут вынесены в отдельную документацию.

### REST API
#### Регистрация и авторизация
1. **`POST /reg`**  
   Регистрирует нового пользователя.  
   **Тело запроса:**
   ```json
   {
       "email": "user@example.com",
       "password": "strongpassword"
   }
   ```
   **Ответ:**
   ```json
   {
       "status": 0,
       "tokens": {
           "access_token": "string",
           "refresh_token": "string"
       }
   }
   ```

2. **`POST /auth`**  
   Авторизует существующего пользователя.
      **Тело запроса:**
   ```json
   {
       "email": "user@example.com",
       "password": "yourpassword"
   }
   ```
   **Ответ:**
   ```json
   {
       "status": 0,
       "tokens": {
           "access_token": "string",
           "refresh_token": "string"
       }
   }
   ``` 

### WebSocket API
WebSocket API используется для взаимодействия в реальном времени, например, для обработки игровых событий, таких как покупка улучшений или приготовление еды.

Подключение:
```
ws://<ваш_домен>:1323/ws
```

#### Структура сообщений

Каждое сообщение к серверу должно включать следующую структуру:

```json
{
    "message_type": "request",
    "request_id": "unique-id",
    "request_type": "action_type",
    "data": {
        "token": "value",
        "id": "value"
    }
}
```

Где:
- message_type: всегда "request".
- request_id: уникальный идентификатор запроса.
- request_type: тип запроса (см. поддерживаемые события ниже).
- data: структура данных запроса с полями token и id(опционально)

Ответ сервера

Сервер формирует ответ следующего вида:

```json
{
    "message_type": "response",
    "request_id": "unique-id",
    "data": {
        /* данные ответа */
    }
}
```

#### Поддерживаемые типы запросов

Сервер обрабатывает следующие типы запросов:
- "cook": событие приготовления блюда, увеличивает количество блюд.
- "sell": событие продажи, увеличивает количество денег и уменьшает количество блюд.
- "upgrade_buy": покупка улучшений.
- "session": получение\инициализация игровой сессии пользователя.
- "level_up": повышение уровня.
- "level_check": получение количество необходимого xp для повышения уровня.
- "session_reset": сброс игровой сессии.
- "passive": автоматическая готовка и продажа блюд.

⚠️ **Важно:** запрос `session` является обязательным начальным запросом.  
Он должен выполняться первым, так как используется для получения или инициализации игровой сессии пользователя при её отсутствии.

#### Пример взаимодействия
**Запрос**:
```json
{
    "message_type": "request",
    "request_id": "1",
    "request_type": "session",
    "data": {
        "token": "string"
    }
}
```

**Ответ:**
```json
{
    "message_type": "response",
    "request_id": "1",
    "request_type": "session",
    "data": {
        "session":
        {
          "user_id":4,
          "user_email":"",
          "money":9,
          "dishes":0,
          "level":
          {
            "rank":4,
            "xp":11.5
          },
          "prestige":
          {
            "current_value":0,
            "current_boost_value":0,
            "accumulated_value":0
          },
          "upgrades":
          {
            "available": [],
            "current": []
          }
        }
    }
}
```
---

## 🏗️ Архитектура

- Backend реализован как монолитное серверное приложение со слоистой архитектурой, что упрощает разработку, деплой и сопровождение проекта.
- REST API используется для обработки клиентских запросов и выполнения операций, не требующих взаимодействия в реальном времени.
- WebSocket API реализует механизм real-time взаимодействия и используется для передачи игровых событий и обновлений состояния сессии между сервером и клиентами.
- Redis применяется для кэширования состояния игровых сессий и временных данных, обеспечивая устойчивость и производительность при разрывах соединения и высокой частоте обновлений.

---

## 🚧 Статус проекта

Проект активно развивается, в перспективе планируется переход к микросервисной архитектуре.

---

## 📄 Лицензия

Проект является учебно-пет-проектом. Коммерческое использование возможно только с разрешения автора.
